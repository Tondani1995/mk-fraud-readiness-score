# Production-specific canonical reconciliation runbook

Status: controller-review artefact only. Do not execute. This runbook is not valid for UAT.

The old numeric-chain repair is complete and superseded. Production now records numeric versions `0001`–`0007` and `0009`. Production also contains the timestamped Phase 9–13 history and the six timestamped records for the early disabled Phase 14 foundation.

The controlled strategy is generated by `scripts/phase14-generate-production-reconciliation.mjs` as `scripts/phase14-production-canonical-reconciliation.sql`. It is deliberately separate from `scripts/phase14-uat-canonical-reconciliation.sql`.

## Preconditions

- Separate controller approval for an exact reviewed commit.
- Exact production ledger and schema inventory must match `docs/v1/phase14/production-history-read-only-evidence-2026-07-15.md`.
- The early Phase 14 tables must exist and `phase14_security_gates` must not exist, unless the schema delta already committed and only its ledger acknowledgement is missing.
- Automatic fulfilment must remain disabled.
- A current backup and change window are controller responsibilities; this PR does not schedule or perform them.

## Transaction

The production artefact takes a production-specific advisory lock and verifies the complete ledger. In one transaction it applies only the missing privileged-grant closure, adversarial closure, webhook state machine, fourth remediation, fifth remediation, sixth remediation and sixth-handoff correction sources. It then replaces only the six timestamped early Phase 14 ledger records with canonical `0017_phase14_canonical_disabled_foundation` acknowledgement.

It does not reapply `0001`–`0016` or the early Phase 14 tables. It does not use the UAT script. It never enables a security gate, feature policy or AI route.

## Recovery and restart

If the final schema exists but canonical `0017` acknowledgement is absent, the script recognizes the exact old production ledger and performs ledger-only acknowledgement recovery. If canonical `0017` already exists, it performs read-only safe-restart posture checks and executes no delta.

## Disposable proof

CI reproduces the exact production versions and names on a disposable local database, verifies the baseline schema inventory digest `417dfbf2fb7fdea1727d7dc9d84d0463a597db6b545def32de18d2e15d8509cd`, seeds non-customer sentinel rows in every early Phase 14 data table, and captures stable-column checksums. It then proves:

1. normal production-history convergence;
2. sentinel preservation;
3. six timestamped Phase 14 records become one canonical `0017` record;
4. a second run is a safe no-op;
5. final-schema/missing-ledger acknowledgement recovery;
6. final schema equivalence to a fresh canonical replay;
7. every gate, feature policy and AI route is disabled.

No step in this proof connects to production or UAT.
