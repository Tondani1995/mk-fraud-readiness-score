# Phase 10 Content Blocks Review Needed

The Claude V2 report pack produced 36 draft content blocks. They are intentionally not active. The application must select only `active` blocks for real client reports, so these blocks require MK review before they can influence a paid report.

This draft PR includes only a minimal migration seed to establish the report template and draft-review markers. The full Claude V2 content pack should be integrated by Codex after review or kept in draft until MK approves final wording.

Do not mass-activate the content blocks without a separate MK approval step.
