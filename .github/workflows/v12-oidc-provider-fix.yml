name: V1.2 Vercel OIDC Provider Fix

on:
  push:
    branches:
      - qa/v12-essential-comparison-20260823
    paths:
      - .github/workflows/v12-oidc-provider-fix.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          ref: qa/v12-essential-comparison-20260823
          fetch-depth: 0

      - name: Allow native Vercel AI Gateway OIDC runtime
        shell: python
        run: |
          from pathlib import Path
          p = Path('src/lib/reports/narrative/whole-manuscript-writer.ts')
          s = p.read_text()
          old = "if (!model || !(process.env.AI_GATEWAY_API_KEY || process.env.VERCEL_AI_GATEWAY_API_KEY || process.env.VERCEL_OIDC_TOKEN)) throw new NarrativeWriterUnavailableError();"
          new = """const runningOnVercel = process.env.VERCEL === '1' || Boolean(process.env.VERCEL_ENV);\n  if (!model || (!(process.env.AI_GATEWAY_API_KEY || process.env.VERCEL_AI_GATEWAY_API_KEY || process.env.VERCEL_OIDC_TOKEN) && !runningOnVercel)) throw new NarrativeWriterUnavailableError();"""
          if old not in s:
            if new in s:
              print('Native Vercel OIDC runtime allowance already applied.')
            else:
              raise SystemExit('Expected provider preflight not found; refusing broad rewrite.')
          else:
            s = s.replace(old, new, 1)
            p.write_text(s)

      - name: Verify narrow auth change
        shell: bash
        run: |
          grep -F "const runningOnVercel" src/lib/reports/narrative/whole-manuscript-writer.ts
          grep -F "process.env.VERCEL_ENV" src/lib/reports/narrative/whole-manuscript-writer.ts
          git diff --check

      - name: Commit narrow fix
        shell: bash
        run: |
          git config user.name "MK V1.2 QA"
          git config user.email "actions@users.noreply.github.com"
          git add src/lib/reports/narrative/whole-manuscript-writer.ts
          git diff --cached --quiet && exit 0
          git commit -m "allow native Vercel OIDC for report provider"
          git push origin HEAD:qa/v12-essential-comparison-20260823
