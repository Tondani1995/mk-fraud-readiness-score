name: Essential V10 Full-Path Recovery Proof

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  frozen:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'The 3x full-path recovery generation completed on 2026-08-23. No push-triggered replay is permitted.'
