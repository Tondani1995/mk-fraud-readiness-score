# Supabase reference

Phase 3 does not run migrations or create Supabase resources.

The approved Phase 2 SQL draft is intentionally kept outside this scaffold package as the migration contract until Phase 4 begins. When Phase 4 starts, copy the approved SQL into `supabase/migrations` and run it only after the implementation blueprint is re-confirmed.
