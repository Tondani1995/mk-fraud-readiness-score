/**
 * Final presentation seam for customer-facing Essential HTML.
 *
 * Semantic wording is no longer rewritten here. The final validator scans high-recall candidates,
 * inherits unchanged manuscript-stage decisions, and fails closed for any new unresolved phrase.
 * Deterministic output-contract repairs remain in essential-commercial-output-closure.ts.
 */
export function closeResidualEssentialGroundingDefects(html: string): string {
  return html;
}
